# Cloudflare Pages 部署指南

## 📋 前置条件

1. **Cloudflare 账号** - 如果没有，请先注册：https://dash.cloudflare.com/sign-up
2. **Node.js** - 已安装（你已经有 v24.15.0）
3. **Wrangler CLI** - 已安装（刚刚帮你安装了）

---

## 🚀 部署步骤

### 方法一：使用部署脚本（推荐）

1. **双击运行** `部署到Cloudflare.bat` 文件
2. 首次运行会要求你登录 Cloudflare：
   - 浏览器会自动打开 Cloudflare 登录页面
   - 登录后授权 Wrangler 访问你的账号
   - 返回命令行窗口，按回车继续
3. 等待部署完成
4. 部署成功后，你的网站地址是：
   ```
   https://caiwenjie-portfolio.pages.dev
   ```

### 方法二：手动命令行部署

```bash
# 1. 进入项目目录
cd d:\vscode\个人网页

# 2. 登录 Cloudflare（首次需要）
wrangler login

# 3. 部署
wrangler pages deploy . --project-name=caiwenjie-portfolio --branch=main
```

---

## 🌐 自定义域名（可选）

如果你想使用自己的域名（如 `caiwenjie.com`）：

1. 登录 Cloudflare Dashboard：https://dash.cloudflare.com
2. 进入 **Workers & Pages** > **Overview**
3. 找到你的项目 `caiwenjie-portfolio`
4. 进入 **Settings** > **Custom domains**
5. 点击 **Set up a custom domain**
6. 输入你的域名
7. 按照提示配置 DNS 记录

---

## 🔄 更新部署

每次修改代码后，重新运行 `部署到Cloudflare.bat` 即可更新网站。

---

## 📁 项目结构

```
个人网页/
├── index.html          # 主页
├── ai-zhanshu.html     # AI斩书项目
├── goall.html          # 校园足球赛事项目
├── aigc-ops.html       # AIGC运营项目
├── PRD-筛选错题组卷导出功能.html  # PRD文档
├── 未来慧学_作品集介绍页.html     # 未来慧学项目
├── css/style.css       # 样式文件
├── js/main.js          # 主脚本
├── vendor/             # 本地依赖库
│   ├── tailwind.js
│   └── lucide.min.js
├── assets/             # 资源文件
│   ├── covers/         # 项目封面图
│   └── logo/           # 工具logo
└── 部署到Cloudflare.bat  # 部署脚本
```

---

## ❓ 常见问题

### Q: 部署后页面是空白的？
A: 检查浏览器控制台是否有错误，确保所有文件都正确上传。

### Q: 如何查看部署日志？
A: 登录 Cloudflare Dashboard，进入 Workers & Pages > 你的项目 > Deployments

### Q: 部署后样式丢失？
A: 确保 `css/style.css` 和 `vendor/tailwind.js` 都正确上传。

### Q: 图片不显示？
A: 检查 `assets/` 目录是否完整上传。

---

## 🎉 部署成功后

你的网站将自动获得：
- ✅ 全球 CDN 加速
- ✅ HTTPS 自动启用
- ✅ 每次推送自动部署
- ✅ 免费 SSL 证书

网站地址：**https://caiwenjie-portfolio.pages.dev**
